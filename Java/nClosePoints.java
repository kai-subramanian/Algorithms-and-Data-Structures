import java.util.*;
public class nClosePoints{
    static double distCalc(int x,int y){
        double d=0;
        d = Math.pow(Math.abs(x),2) + Math.pow(Math.abs(y),2);
        return Math.sqrt(d);

    }
    public static int[][] nClosestPoints(int[][] points, int n) {
        // add your code here
        HashMap<Integer[],Integer> hm=new HashMap<Integer[],Integer>();      
    }
}